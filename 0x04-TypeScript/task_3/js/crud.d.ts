import { RowID, RowElement } from "./interface";

/**
 * type declarations for each crud function
 */

export function insertRow(row: RowElement): RowID {
  /** function insert row  */
}

export function updateRow(newRowID: number, updatedRow: RowElement): number {
  /** function update row  */
}

export function deleteRow(newRowID: number): void {
  /** function delete row  */
}
